import { FormBuilderClient } from "./form-builder-client";

export const metadata = {
  title: "Visitor form-builder",
};

export default function VisitorFormBuilderPage() {
  return <FormBuilderClient />;
}
